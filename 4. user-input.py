name = input("Enter your Name: ")
# Create a variable called name, and asks for input from the user
age = input("Enter your Age: ")
# Create a variable called age, and asks for input from the user

print("Hello " + name + "! You are " + age)
# Outputs the data entered by the user to the screen using the values the user entered which are stored in the variables
